import SectionTemplate from "../components/SectionTemplate";

const Food = () => {
  return (
    <SectionTemplate>
      <h2>Metabolic Rounds</h2>
      <p>Content for Metabolic Rounds goes here.</p>
    </SectionTemplate>
  );
};

export default Food;
