import SectionTemplate from "../components/SectionTemplate";

const OHP = () => {
  return (
    <SectionTemplate>
      <h2>Occupational Health Program</h2>
      <p>Content for Occupational Health Program goes here.</p>
    </SectionTemplate>
  );
};

export default OHP;
