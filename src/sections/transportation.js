import SectionTemplate from "../components/SectionTemplate";

const Transportation = () => {
  return (
    <SectionTemplate>
      <h2>Transportation Rounds</h2>
      <p>Content for Transportation Rounds goes here.</p>
    </SectionTemplate>
  );
};

export default Transportation;
